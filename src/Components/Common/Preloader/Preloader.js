import React from 'react';
import preloader from "../../../assets/img/preloader.svg";
import {ProfileImage} from "../../../Style/style";

const Preloader = (props) => {
    return <ProfileImage src={preloader}/>;
};

export default Preloader;